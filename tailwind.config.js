/** @type {import('tailwindcss').Config} */
module.exports = {
    // Wskaż ścieżki do plików, które zawierają klasy Tailwind
    content: [
        "./src/**/*.{js,jsx,ts,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}